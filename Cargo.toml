[package]
name = "tls-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = { version = "1.0.100" }
num_enum = "0.7.5"
tokio = { version = "1.48.0", default-features = false, features = ["rt-multi-thread", "macros", "net", "time"]}
